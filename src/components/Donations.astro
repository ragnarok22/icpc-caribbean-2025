---
import { DONATION_CARDS } from "@/lib/data";

// Montos reales de donaciones (no confundir con patrocinios)
const DONATION_AMOUNTS = {
  usd: 32.6,
  cup: 2150,
};

// Usar formato de fecha simple para evitar problemas de zona horaria
const formattedDeadline = "5 de noviembre de 2025";

const usdFormatter = new Intl.NumberFormat("es-ES", {
  style: "currency",
  currency: "USD",
  minimumFractionDigits: 2,
  maximumFractionDigits: 2,
});

const cupFormatter = new Intl.NumberFormat("es-ES", {
  style: "currency",
  currency: "CUP",
  minimumFractionDigits: 0,
  maximumFractionDigits: 0,
});

const formattedUSD = usdFormatter.format(DONATION_AMOUNTS.usd);
const formattedCUP = cupFormatter.format(DONATION_AMOUNTS.cup);
---

<section class="bg-blue py-16 text-white" id="donaciones">
  <div class="mx-auto flex max-w-6xl flex-col gap-12 px-4 sm:px-6">
    <header class="text-center">
      <p class="text-sm font-semibold tracking-wide text-white/80 uppercase">
        Apoya la sede caribeña del ICPC
      </p>
      <h2 class="mt-3 text-3xl font-semibold text-white sm:text-4xl">
        ¿Cómo puedes contribuir?
      </h2>
      <p class="mx-auto mt-3 max-w-3xl text-base text-white/80 sm:text-lg">
        Cualquier donación, grande o pequeña, ayuda a cubrir la logística,
        hospedaje y recursos necesarios para que nuestros equipos representen a
        la región. Estas son las vías oficiales para apoyar el evento.
      </p>
    </header>

    <div class="grid gap-6 lg:grid-cols-2">
      {
        DONATION_CARDS.map((card) => (
          <article
            class="group rounded-xl border border-white/20 bg-white/10 p-6 shadow-sm backdrop-blur-sm transition-transform duration-300 hover:-translate-y-1 hover:bg-white/15"
            id={`donation-${card.id}`}
          >
            <h3 class="text-lg font-semibold text-white">{card.title}</h3>
            <p class="mt-2 text-sm text-white/80">{card.description}</p>

            <ul class="mt-4 space-y-3 text-sm text-white/80">
              {card.entries.map((entry) => {
                const valueClasses = [
                  "ml-1",
                  entry.isMonospace ? "font-mono text-sm" : "",
                ]
                  .filter(Boolean)
                  .join(" ");

                return (
                  <li class="flex flex-col gap-2 rounded-lg bg-white/5 p-4 transition-colors duration-300 group-hover:bg-white/10 sm:flex-row sm:items-center sm:justify-between">
                    <div>
                      <span class="font-medium text-white/90">
                        {entry.label}:
                      </span>
                      <span class={valueClasses}>{entry.display}</span>
                    </div>
                    <button
                      type="button"
                      class="inline-flex items-center justify-center rounded-md border border-white/30 px-3 py-1 text-xs font-semibold tracking-wide text-white/90 uppercase transition hover:bg-white/20 focus-visible:outline focus-visible:outline-offset-2 focus-visible:outline-white"
                      data-copy={entry.copy}
                      data-original-label="Copiar"
                      aria-label={entry.ariaLabel}
                    >
                      Copiar
                    </button>
                  </li>
                );
              })}
            </ul>
          </article>
        ))
      }
    </div>

    <div
      class="rounded-2xl border border-white/20 bg-white/10 p-8 backdrop-blur-sm"
    >
      <h3 class="text-xl font-semibold text-white">
        Impacto de las donaciones
      </h3>
      <p class="mt-2 text-sm text-white/80">
        Gracias al apoyo de personas solidarias, hemos recibido donaciones que
        nos permiten planificar la participación de la delegación caribeña. Las
        donaciones son independientes de los patrocinios oficiales.
      </p>

      <dl class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        <div class="rounded-lg bg-white/10 p-4 shadow-sm">
          <dt class="text-sm font-medium text-white/70">Donaciones en USD</dt>
          <dd class="mt-1 text-2xl font-semibold text-white">
            {formattedUSD}
          </dd>
        </div>

        <div class="rounded-lg bg-white/10 p-4 shadow-sm">
          <dt class="text-sm font-medium text-white/70">Donaciones en CUP</dt>
          <dd class="mt-1 text-2xl font-semibold text-white">
            {formattedCUP}
          </dd>
        </div>

        <div class="rounded-lg bg-white/10 p-4 shadow-sm">
          <dt class="text-sm font-medium text-white/70">
            Fecha límite de donación
          </dt>
          <dd class="mt-1 text-lg font-semibold text-white">
            {formattedDeadline}
          </dd>
        </div>
      </dl>
    </div>
  </div>
</section>

<script>
  if (typeof window !== "undefined") {
    const buttons = document.querySelectorAll("[data-copy]");

    const copyToClipboard = async (text) => {
      if (navigator.clipboard?.writeText) {
        await navigator.clipboard.writeText(text);
        return;
      }

      const textArea = document.createElement("textarea");
      textArea.value = text;
      textArea.setAttribute("readonly", "");
      textArea.style.position = "fixed";
      textArea.style.top = "-1000px";
      textArea.style.left = "-1000px";
      document.body.appendChild(textArea);
      textArea.focus();
      textArea.select();
      document.execCommand("copy");
      document.body.removeChild(textArea);
    };

    buttons.forEach((button) => {
      const originalLabel =
        button.getAttribute("data-original-label") ||
        button.textContent ||
        "Copiar";
      button.addEventListener("click", async () => {
        const text = button.getAttribute("data-copy");
        if (!text) return;

        try {
          await copyToClipboard(text);
          button.textContent = "Copiado";
          button.classList.add("bg-white/20");
          setTimeout(() => {
            button.textContent = originalLabel.trim();
            button.classList.remove("bg-white/20");
          }, 2000);
        } catch (error) {
          console.error("Error al copiar al portapapeles:", error);
          button.textContent = "Error";
          setTimeout(() => {
            button.textContent = originalLabel.trim();
            button.classList.remove("bg-white/20");
          }, 2000);
        }
      });
    });
  }
</script>
